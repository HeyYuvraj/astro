---
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import Link from "@components/Link.astro";
import { SITE, HOME, SOCIALS } from "@consts";
---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <div class="max-w-3xl mx-auto">
      <div class="space-y-16">
        <!-- Hero Section -->
        <section class="space-y-6">
          <div>
            <h1 class="text-5xl md:text-6xl font-bold text-black dark:text-white mb-3 tracking-tight">
              Yuvraj Verma
            </h1>
            <p class="text-xl md:text-2xl font-medium bg-gradient-to-r from-purple-600 to-pink-600 dark:from-purple-400 dark:to-pink-400 bg-clip-text text-transparent">
              Developer Advocate / Technical Educator
            </p>
          </div>
          <p class="text-lg leading-relaxed text-black/80 dark:text-white/80">
            I work with backend systems, documentation, and developer experience to help developers build better applications.
          </p>
          
          <!-- Social Links -->
          <div class="flex flex-wrap gap-4 items-center pt-2">
            {SOCIALS.map(social => (
              <a
                href={social.HREF}
                target="_blank"
                rel="noopener noreferrer"
                aria-label={`${SITE.NAME} on ${social.NAME}`}
                class="inline-flex items-center gap-2 px-4 py-2 rounded-lg border border-black/10 dark:border-white/20 hover:border-purple-500 dark:hover:border-purple-400 hover:bg-purple-50 dark:hover:bg-purple-950/30 transition-all duration-200"
              >
                <span class="capitalize">{social.NAME}</span>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                </svg>
              </a>
            ))}
          </div>
        </section>

        <!-- Divider -->
        <div class="border-t border-black/10 dark:border-white/10"></div>

        <!-- Why Appwrite Section -->
        <section class="space-y-6">
          <h2 class="text-3xl md:text-4xl font-bold text-black dark:text-white">
            Why Appwrite
          </h2>
          <div class="relative">
            <div class="absolute inset-0 bg-gradient-to-r from-purple-500/10 to-pink-500/10 dark:from-purple-500/5 dark:to-pink-500/5 rounded-2xl blur-xl"></div>
            <div class="relative p-8 rounded-2xl border border-black/10 dark:border-white/10 bg-white/50 dark:bg-black/20 backdrop-blur-sm">
              <p class="text-lg leading-relaxed text-black/80 dark:text-white/80">
                Appwrite stands out because it approaches backend development from a practical developer point of view. It brings commonly required backend services like <span class="font-semibold text-purple-600 dark:text-purple-400">authentication, databases, storage, serverless functions, realtime, and messaging</span> into a single, well-integrated platform, instead of making developers manage multiple tools and services.
              </p>
              <p class="text-lg leading-relaxed text-black/80 dark:text-white/80 mt-4">
                Being <span class="font-semibold text-pink-600 dark:text-pink-400">open source and self-hostable</span> gives developers control over their data and infrastructure, while the clear APIs and documentation make the system easier to understand, debug, and maintain. This focus on reducing backend complexity without hiding important details is what makes Appwrite genuinely useful for building real-world applications.
              </p>
            </div>
          </div>
        </section>
      </div>
    </div>
  </Container>
</PageLayout>
